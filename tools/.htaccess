# Skrypty pomocnicze uruchamiane z linii poleceń — nie przez przeglądarkę
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
