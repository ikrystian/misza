# Pliki z treścią strony (JSON) czyta wyłącznie PHP — nie udostępniamy ich przez HTTP
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
